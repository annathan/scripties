# secrets.yaml — rename to secrets.yaml in your HA config directory
# NEVER commit the real secrets.yaml to git.
# This file shows the keys referenced across all configs.

home_latitude: "-33.0000"         # your home GPS latitude
home_longitude: "151.0000"        # your home GPS longitude

mqtt_username: "homeassistant"
mqtt_password: "changeme"

wifi_ssid: "YourIoTWifi"
wifi_password: "changeme"

ap_fallback_password: "changeme"  # ESPHome fallback AP password

esphome_api_key: ""               # generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
esphome_ota_password: "changeme"

alarm_code: "1234"                # PIN to disarm the alarm panel; use 6+ digits
